<div class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-4">Consultation Details</h1>
  <p><strong>Client:</strong> <%= @consultation.client.first_name %> <%= @consultation.client.last_name %></p>
  <p><strong>Practitioner:</strong> <%= @consultation.practitioner.first_name %> <%= @consultation.practitioner.last_name %></p>
  <p><strong>Notes:</strong></p>
  <div class="bg-gray-100 rounded-md p-4">
    <%= simple_format(@consultation.notes) %>
  </div>
  <%= link_to 'Edit', edit_consultation_path(@consultation), class: 'mt-4 inline-block px-4 py-2 font-bold text-white bg-blue-500 rounded-md hover:bg-blue-600 focus:outline-none focus:ring focus:ring-blue-300 focus:ring-opacity-50' %>
</div>